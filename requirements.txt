fastapi>=0.141.1
uvicorn[standard]>=0.53.0
yt-dlp>=2026.8.19
# 内置 ffmpeg 二进制，用于合并音视频（B站/YouTube 高清必需），零配置无需单独安装
imageio-ffmpeg>=0.6.0
# 抖音专用解析模块的 HTTP 客户端
requests>=2.34.2
# AI 总结：调用大模型（OpenAI 兼容端点）的 HTTP 客户端
httpx>=0.27.0
# 从 .env 读取 AI_PROVIDER / API Key 等配置（可选，未装则仅用系统环境变量）
python-dotenv>=1.0.0
# 阶段0 内容缓存持久化：ORM 抽象（默认 SQLite，阶段1改 DATABASE_URL 即迁 Postgres）
SQLAlchemy>=2.0
# PostgreSQL 驱动（psycopg3）：DATABASE_URL 指向 postgresql+psycopg:// 时使用；默认 SQLite 无需
psycopg[binary]>=3.1
# 多租户认证地基：密码哈希（argon2id，业界推荐）；未安装时 security.py 自动回退 PBKDF2
argon2-cffi>=23.1.0
# 头像等文件上传：FastAPI 的 File/UploadFile（multipart 表单）依赖此包
python-multipart>=0.0.20
